# Adapter Capability Matrix

| Adapter         | Capabilities                |
|-----------------|----------------------------|
| PriceAdapter    | Fetches and normalizes price data |
| MeterAdapter    | Fetches and normalizes meter data |
| DeviceAdapter   | Fetches and normalizes device data |

# Documentation

- See `src/models/` for data models
- See `src/lib/core/adapters/` for adapter implementations
- See `src/services/` for orchestration and logging
- See `src/homey/` for Homey integration
